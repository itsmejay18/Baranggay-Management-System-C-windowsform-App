<UserControl x:Class="baranggaysystem1.Views.Pages.RolePermissionsPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp" Background="{StaticResource PageBgBrush}" xmlns:pages="clr-namespace:baranggaysystem1.Views.Pages;assembly=baranggaysystem1">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="36" />
    </Grid.RowDefinitions>
    <Border Grid.Row="0" Style="{StaticResource PageHeaderStyle}">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Horizontal">
          <Border Width="24" Height="24" CornerRadius="10" Background="#FEF2F2" Margin="0,0,8,0">
            <fa:IconBlock Icon="Key" Foreground="#EF4444" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Roles &amp; Permissions" Style="{StaticResource HeadingTextStyle}" />
            <TextBlock Name="recordCountLabel" Text="Manage access control roles and permissions" Style="{StaticResource SubtitleTextStyle}" Margin="0,2,0,0" />
          </StackPanel>
        </StackPanel>
        <Button Name="btnAddRole" Grid.Column="1" Style="{StaticResource PrimaryButtonStyle}" MinWidth="140" Height="40" Click="BtnAdd_Click">
          <StackPanel Orientation="Horizontal">
            <fa:IconBlock Icon="Plus" FontSize="12" Margin="0,0,8,0" VerticalAlignment="Center" />
            <TextBlock Text="Create Role" VerticalAlignment="Center" />
          </StackPanel>
        </Button>
      </Grid>
    </Border>
    <Border Grid.Row="1" Style="{StaticResource ToolbarBorderStyle}">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="320" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Background="#FFFFFFFF" CornerRadius="6" BorderBrush="{StaticResource Slate300Brush}" BorderThickness="1" Height="36">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="36" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <fa:IconBlock Grid.Column="0" Icon="Search" FontSize="13" Foreground="{StaticResource Slate500Brush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <TextBox Name="searchBox" Grid.Column="1" BorderThickness="0" Background="#00FFFFFF" VerticalContentAlignment="Center" FontSize="13" Margin="0,0,6,0" TextChanged="SearchBox_TextChanged" />
          </Grid>
        </Border>
        <Button Name="btnRefresh" Grid.Column="2" Style="{StaticResource SecondaryButtonStyle}" Height="36" MinWidth="0" Padding="12,0" Click="BtnRefresh_Click">
          <StackPanel Orientation="Horizontal">
            <fa:IconBlock Icon="SyncAlt" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center" />
            <TextBlock Text="Refresh" VerticalAlignment="Center" />
          </StackPanel>
        </Button>
      </Grid>
    </Border>
    <Border Grid.Row="2" Background="#FFFFFFFF" Margin="20,14,20,0" CornerRadius="8" BorderBrush="{StaticResource Slate100Brush}" BorderThickness="1" ClipToBounds="True">
      <Grid>
        <DataGrid Name="mainGrid" Style="{StaticResource StyledDataGridStyle}" ColumnHeaderStyle="{StaticResource StyledDataGridColumnHeaderStyle}" RowStyle="{StaticResource StyledDataGridRowStyle}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionChanged="MainGrid_SelectionChanged" MouseDoubleClick="MainGrid_MouseDoubleClick">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Role" Width="180" Binding="{Binding Name}" />
            <DataGridTextColumn Header="Description" Width="*" Binding="{Binding Description}" />
            <DataGridTextColumn Header="Active Users" Width="120" Binding="{Binding ActiveUserCount, StringFormat={}{0:N0}}" />
            <DataGridTextColumn Header="Assigned Users" Width="130" Binding="{Binding UserCount, StringFormat={}{0:N0}}" />
          </DataGrid.Columns>
        </DataGrid>
        <StackPanel Name="emptyState" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
          <fa:IconBlock Icon="Key" FontSize="48" Foreground="{StaticResource Slate300Brush}" HorizontalAlignment="Center" Margin="0,0,0,12" />
          <TextBlock Name="emptyLabel" Text="No roles defined." Style="{StaticResource SubtitleTextStyle}" HorizontalAlignment="Center" />
        </StackPanel>
      </Grid>
    </Border>
    <Border Name="contextActionBar" Grid.Row="3" Visibility="Collapsed" Background="{StaticResource Slate100Brush}" Margin="20,10,20,0" Padding="16,10" CornerRadius="8" BorderBrush="{StaticResource Slate200Brush}" BorderThickness="1">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
          <fa:IconBlock Icon="UserShield" Foreground="#EF4444" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0" />
          <TextBlock Text="Selected: " VerticalAlignment="Center" FontWeight="Bold" Foreground="{StaticResource Slate700Brush}" />
          <StackPanel VerticalAlignment="Center">
            <TextBlock Name="selectedRecordLabel" Text="Role" FontWeight="Bold" Foreground="{StaticResource Slate900Brush}" Margin="4,0,20,0" MaxWidth="260" TextTrimming="CharacterEllipsis" />
            <TextBlock Name="selectedRoleMetaLabel" Text="Role details" Style="{StaticResource SmallTextStyle}" Margin="4,2,20,0" MaxWidth="360" TextTrimming="CharacterEllipsis" />
          </StackPanel>
        </StackPanel>
        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
          <Button Name="btnEditRole" Style="{StaticResource ContextualActionButtonStyle}" Margin="0,0,8,0" Click="BtnEdit_Click">
            <StackPanel Orientation="Horizontal">
              <fa:IconBlock Icon="Edit" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center" />
              <TextBlock Text="Edit Permissions" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Name="btnDeleteRole" Style="{StaticResource ActionButtonStyle}" Margin="0,0,8,0" Click="BtnDelete_Click">
            <StackPanel Orientation="Horizontal">
              <fa:IconBlock Icon="TrashAlt" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center" />
              <TextBlock Text="Delete" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Style="{StaticResource GhostButtonStyle}" Padding="8,4" Foreground="{StaticResource Slate400Brush}" Click="BtnClearSelection_Click">
            <fa:IconBlock Icon="Times" FontSize="12" />
          </Button>
        </StackPanel>
      </Grid>
    </Border>
    <Border Grid.Row="4" Margin="20,6,20,14" Background="#00FFFFFF">
      <TextBlock Name="footerCountLabel" Text="No records loaded." Style="{StaticResource SmallTextStyle}" VerticalAlignment="Center" />
    </Border>
  </Grid>
</UserControl>
